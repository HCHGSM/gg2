# Desktop application (PySide6 GUI)
PySide6>=6.6,<7.0

# Shared core (used by both desktop and web app)
SQLAlchemy>=2.0,<3.0
bcrypt>=4.0,<5.0

# Web application (FastAPI)
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
jinja2>=3.1,<4.0
python-multipart>=0.0.9   # required by FastAPI Form(...) parsing
pydantic>=2.0,<3.0

# Reporting / exports (used by pos_erp.services.report_service / import_export_service)
openpyxl>=3.1,<4.0
reportlab>=4.0,<5.0

# Packaging
pyinstaller>=6.0,<7.0

# Testing
pytest>=8.0,<9.0
